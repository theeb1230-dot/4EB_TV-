name: tvmaze_provider
description: Lawful public TV metadata discovery provider for 4BA.
version: 0.0.1
publish_to: none
environment:
  sdk: ">=3.4.0 <4.0.0"
dependencies:
  core_domain:
    path: ../core_domain
  provider_sdk:
    path: ../provider_sdk
  http: ^1.2.2
dev_dependencies:
  test: ^1.25.0
