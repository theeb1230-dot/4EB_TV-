# metadata_engine

Provider-independent metadata identity and conservative deduplication for 4BA.

Matching prefers shared external IDs. Without IDs, content type must match and conflicting known years reject title matching. Unicode title normalization preserves Arabic, English and Turkish letters/numbers and performs no transliteration.

No remote credentials, network access, ads or personal telemetry are present.
