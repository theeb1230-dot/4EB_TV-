name: app_flow
description: 4BA user journey state and playback coordination.
version: 0.1.0
publish_to: none
environment:
  sdk: ">=3.4.0 <4.0.0"
dependencies:
  core_domain:
    path: ../core_domain
  playback_orchestrator:
    path: ../playback_orchestrator
dev_dependencies:
  test: ^1.25.0
