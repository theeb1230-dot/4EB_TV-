name: local_data_memory
description: In-memory local-first adapters for 4BA Core contracts.
version: 0.0.1
publish_to: none
environment:
  sdk: ">=3.4.0 <4.0.0"
dependencies:
  core_domain:
    path: ../core_domain
dev_dependencies:
  test: ^1.25.0
