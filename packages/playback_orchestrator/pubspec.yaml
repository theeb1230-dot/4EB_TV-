name: playback_orchestrator
description: Provider-independent resolve-to-play orchestration for 4BA.
version: 0.1.0
publish_to: none
environment:
  sdk: ">=3.4.0 <4.0.0"
dependencies:
  core_domain:
    path: ../core_domain
  provider_sdk:
    path: ../provider_sdk
  resolver_engine:
    path: ../resolver_engine
dev_dependencies:
  test: ^1.25.0
